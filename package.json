{
  "name": "social_blendr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm --stream -r run dev",
    "init": " cd apps/backend && node ace db:wipe && node ace migration:run && node ace db:truncate && node ace db:seed && cd ../.."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
